# ===========================================
# 学生积分管理系统 - 环境变量配置示例
# ===========================================
# 复制此文件为 .env 并填写实际值
# cp .env.example .env

# -------------------------------------------
# 后端配置
# -------------------------------------------

# 项目名称
PROJECT_NAME=学生积分管理

# MySQL 数据库连接（必填）
# 格式：mysql+aiomysql://用户名:密码@主机:端口/数据库名
# 注意：如果密码包含特殊字符，需要进行 URL 编码
# socker-compose里边会自动构造此配置项
# 此配置会在docker compose中构造出来
# SQLALCHEMY_DATABASE_URI==mysql+aiomysql://app-points:points%407225@127.0.0.1:3306/student_points_db

# JWT 密钥（必填）
# 用于签发和验证 JWT Token，请使用随机生成的长字符串
# 可以使用命令生成：openssl rand -hex 32
SECRET_KEY=ZAAL7225

# JWT Token 过期时间（分钟），默认 7 天
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# JWT 算法，默认 HS256
ALGORITHM=HS256

# 允许的前端跨域地址（CORS）
# 支持逗号分隔的多个地址，或 JSON 数组格式
# 开发环境示例：
BACKEND_CORS_ORIGINS=http://8.130.97.135,https://score.fanhaoyu.xyz
# BACKEND_CORS_ORIGINS=https://score.dfanhaoyu.xyz
# 生产环境示例：
# BACKEND_CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com

# -------------------------------------------
# 前端配置（Vite 环境变量，以 VITE_ 开头）
# -------------------------------------------

# 后端 API 地址
# 生产环境：
VITE_API_BASE_URL=https://score.fanhaoyu.xyz/api/v1
# 生产环境：
# VITE_API_BASE_URL=https://api.your-domain.com/api/v1

# -------------------------------------------
# Docker / 生产环境配置
# -------------------------------------------

# MySQL 配置（用于 docker-compose）
MYSQL_ROOT_PASSWORD=root7225
MYSQL_DATABASE=student_points_db
MYSQL_USER=app-points
MYSQL_PASSWORD=points7225
MYSQL_DATA_DIR=./data/mysql

# 服务端口
BACKEND_PORT=8000
FRONTEND_PORT=80


# DeepSeek（推荐）
AI_API_KEY=sk-ca06ad5cee4a4ac7a8d577ceb5f66f1f
AI_API_BASE_URL=https://api.deepseek.com
AI_MODEL=deepseek-chat

# 或者使用通义千问
# AI_API_KEY=your-qwen-api-key
# AI_API_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# AI_MODEL=qwen-turbo
